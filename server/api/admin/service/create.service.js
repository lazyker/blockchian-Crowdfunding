'use strict';

const { web3 } = require('../../../web3');

/**
 * 크라운드펀딩 등록
 *
 * @param {Object} body
 * @param {String} body.companyName - 회사이름
 * @param {String} body.startDate   - 시작일
 * @param {String} body.endDate     - 종료일
 * @param {String} body.targetEth   - 목표 이더
 * @return {Promise.<void>}
 */
module.exports.exec = async (body) => {

};

